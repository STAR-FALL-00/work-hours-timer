<ui:FluentWindow x:Class="WorkHoursTimer.StatisticsWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="工时统计"
                 Width="800"
                 Height="600"
                 WindowStartupLocation="CenterOwner"
                 WindowBackdropType="Mica"
                 ExtendsContentIntoTitleBar="True"
                 Topmost="True">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 标题栏 -->
        <ui:TitleBar Grid.Row="0" Title="工时统计"/>
        
        <!-- 标题和时间范围选择 -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0">
                <TextBlock Text="📊 工时统计"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="{StaticResource TextPrimary}"/>
                <TextBlock x:Name="SubtitleText"
                           Text="查看您的工作时间统计"
                           FontSize="12"
                           Foreground="{StaticResource TextSecondary}"
                           Margin="0,4,0,0"/>
            </StackPanel>
            
            <StackPanel Grid.Column="1"
                        Orientation="Horizontal">
                <ui:Button Content="今日"
                           Width="80"
                           Margin="0,0,8,0"
                           Click="ShowToday_Click"/>
                <ui:Button Content="本周"
                           Width="80"
                           Margin="0,0,8,0"
                           Click="ShowWeek_Click"/>
                <ui:Button Content="本月"
                           Width="80"
                           Margin="0,0,8,0"
                           Click="ShowMonth_Click"/>
                <ui:Button Content="导出"
                           Width="80"
                           Appearance="Primary"
                           Click="Export_Click"/>
            </StackPanel>
        </Grid>
        
        <!-- 统计内容 -->
        <ScrollViewer Grid.Row="2"
                      VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- 总览卡片 -->
                <Border Background="#20FFFFFF"
                        BorderBrush="{StaticResource AccentGold}"
                        BorderThickness="2"
                        CornerRadius="12"
                        Padding="20"
                        Margin="0,0,0,16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- 总工时 -->
                        <StackPanel Grid.Column="0"
                                    HorizontalAlignment="Center">
                            <TextBlock Text="⏱️ 总工时"
                                       FontSize="14"
                                       Foreground="{StaticResource TextSecondary}"
                                       HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TotalHoursText"
                                       Text="0.0 小时"
                                       FontSize="32"
                                       FontWeight="Bold"
                                       Foreground="{StaticResource AccentGold}"
                                       HorizontalAlignment="Center"
                                       Margin="0,8,0,0"/>
                        </StackPanel>
                        
                        <!-- 会话数 -->
                        <StackPanel Grid.Column="1"
                                    HorizontalAlignment="Center">
                            <TextBlock Text="📝 会话数"
                                       FontSize="14"
                                       Foreground="{StaticResource TextSecondary}"
                                       HorizontalAlignment="Center"/>
                            <TextBlock x:Name="SessionCountText"
                                       Text="0"
                                       FontSize="32"
                                       FontWeight="Bold"
                                       Foreground="{StaticResource TextPrimary}"
                                       HorizontalAlignment="Center"
                                       Margin="0,8,0,0"/>
                        </StackPanel>
                        
                        <!-- 平均时长 -->
                        <StackPanel Grid.Column="2"
                                    HorizontalAlignment="Center">
                            <TextBlock Text="📈 平均时长"
                                       FontSize="14"
                                       Foreground="{StaticResource TextSecondary}"
                                       HorizontalAlignment="Center"/>
                            <TextBlock x:Name="AverageTimeText"
                                       Text="0.0 小时"
                                       FontSize="32"
                                       FontWeight="Bold"
                                       Foreground="{StaticResource TextPrimary}"
                                       HorizontalAlignment="Center"
                                       Margin="0,8,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- 按项目统计 -->
                <Border Background="#20FFFFFF"
                        BorderBrush="{StaticResource TextSecondary}"
                        BorderThickness="1"
                        CornerRadius="12"
                        Padding="20"
                        Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="📁 按项目统计"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource TextPrimary}"
                                   Margin="0,0,0,16"/>
                        
                        <ItemsControl x:Name="ProjectStatsListBox">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#10FFFFFF"
                                            BorderBrush="{StaticResource TextSecondary}"
                                            BorderThickness="1"
                                            CornerRadius="8"
                                            Padding="16"
                                            Margin="0,0,0,8">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding ProjectName}"
                                                           FontSize="14"
                                                           FontWeight="Bold"
                                                           Foreground="{StaticResource TextPrimary}"/>
                                                <TextBlock FontSize="11"
                                                           Foreground="{StaticResource TextSecondary}"
                                                           Margin="0,4,0,0">
                                                    <Run Text="{Binding SessionCount, Mode=OneWay}"/>
                                                    <Run Text=" 个会话"/>
                                                </TextBlock>
                                            </StackPanel>
                                            
                                            <TextBlock Grid.Column="1"
                                                       Text="{Binding FormattedTotal, Mode=OneWay}"
                                                       FontSize="16"
                                                       FontWeight="Bold"
                                                       Foreground="{StaticResource AccentGold}"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,16,0"/>
                                            
                                            <TextBlock Grid.Column="2"
                                                       FontSize="12"
                                                       Foreground="{StaticResource TextSecondary}"
                                                       VerticalAlignment="Center">
                                                <Run Text="{Binding TotalHours, StringFormat={}{0:F1}, Mode=OneWay}"/>
                                                <Run Text=" 小时"/>
                                            </TextBlock>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        
                        <TextBlock x:Name="NoDataText"
                                   Text="暂无数据"
                                   FontSize="14"
                                   Foreground="{StaticResource TextSecondary}"
                                   HorizontalAlignment="Center"
                                   Margin="0,20,0,0"
                                   Visibility="Collapsed"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</ui:FluentWindow>
