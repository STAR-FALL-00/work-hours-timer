# 可用动画资源清单

## 🦸 勇者（Hero Knight）动画

### 已切割为单帧（可直接使用）
| 动画名称 | 帧数 | 用途建议 | 适合场景 |
|---------|------|---------|---------|
| **Idle** | 8帧 | ✅ 已使用 | 待机、休息 |
| **Attack1** | 6帧 | ✅ 已使用 | 普通攻击 |
| **Attack2** | 6帧 | 💡 推荐 | 连击攻击 |
| **Attack3** | 8帧 | 💡 推荐 | 重击攻击 |
| **Run** | 10帧 | ✅ 已使用 | 跑步、移动 |
| **Roll** | 9帧 | 💡 推荐 | 翻滚、闪避 |
| **Jump** | 3帧 | 可选 | 跳跃 |
| **Fall** | 4帧 | 可选 | 下落 |
| **Hurt** | 3帧 | 可选 | 受伤 |
| **Block** | 5帧 | 可选 | 格挡 |
| **BlockIdle** | 8帧 | 可选 | 格挡待机 |
| **Death** | 10帧 | 可选 | 死亡（失败时） |
| **LedgeGrab** | 5帧 | 可选 | 攀爬 |
| **WallSlide** | 5帧 | 可选 | 墙壁滑行 |

### 推荐工作动画组合

#### 方案 1：战斗风格（当前使用）
- 待机：Idle
- 工作：Attack1、Run、Death（随机切换）

#### 方案 2：多样战斗
- 待机：Idle
- 工作：Attack1、Attack2、Attack3、Roll（随机切换）
- 特点：更多攻击变化，更有战斗感

#### 方案 3：动作冒险
- 待机：Idle
- 工作：Run、Roll、Jump（随机切换）
- 特点：强调移动和闪避，更轻快

#### 方案 4：全动作
- 待机：Idle
- 工作：Attack1、Attack2、Run、Roll（随机切换）
- 特点：攻击+移动，最丰富

---

## 👾 Boss 史莱姆（Slime Enemy）动画

### 已切割为单帧（可直接使用）
| 动画名称 | 帧数 | 用途建议 | 适合场景 |
|---------|------|---------|---------|
| **Idle** | 7帧 | ✅ 已使用 | 待机 |
| **Hurt** | 11帧 | ✅ 已使用 | 受击 |
| **Death** | 14帧 | ✅ 已使用 | 死亡 |

### 未切割（需要处理）
| 动画名称 | 说明 | 处理方法 |
|---------|------|---------|
| **Jump** | 多个跳跃阶段 | 需要用 Python 脚本切割 |

### Jump 动画详情
- Jump Start-up（起跳准备）
- Jump Up（向上跳）
- Jump to Fall（跳跃到下落）
- Jump Down（下落）
- Jump Land（落地）

---

## 🎨 动画效果分析

### 当前问题诊断
如果动画看起来"奇怪"，可能是以下原因：

1. **Death 动画不适合工作场景**
   - Death（死亡）动画是勇者倒下的动作
   - 在工作时播放死亡动画会显得很奇怪
   - 建议：移除 Death，改用其他动作

2. **动画切换频率**
   - 当前 5 秒切换一次
   - 可能太快或太慢
   - 建议：调整为 3-8 秒

3. **动画组合不协调**
   - Attack + Run + Death 组合不太自然
   - 建议：使用更协调的组合

---

## 💡 推荐改进方案

### 方案 A：纯战斗风格（推荐）
```
勇者工作动画：
- Attack1（普通攻击）
- Attack2（连击）
- Attack3（重击）
- Roll（翻滚闪避）

Boss 反应动画：
- Hurt（受击）
- Idle（待机，偶尔）
```
**特点**：战斗感强，动作连贯

### 方案 B：移动+战斗
```
勇者工作动画：
- Attack1（攻击）
- Attack2（攻击）
- Run（跑步）
- Roll（翻滚）

Boss 反应动画：
- Hurt（受击）
- Idle（待机）
```
**特点**：动静结合，更有活力

### 方案 C：轻快风格
```
勇者工作动画：
- Run（跑步）
- Roll（翻滚）
- Jump（跳跃）

Boss 反应动画：
- Idle（待机为主）
- Hurt（偶尔受击）
```
**特点**：轻松愉快，不那么激烈

---

## 🔧 如何更换动画

### 1. 修改 ViewModel
编辑 `wpf_app/WorkHoursTimer/ViewModels/WidgetViewModel.cs`

找到动画定义部分，修改为：

```csharp
// 例如：使用方案 A（纯战斗风格）
_heroAttackFrames = Enumerable.Range(0, 6)
    .Select(i => $"pack://application:,,,/Assets/Images/Hero/Hero Knight/Sprites/HeroKnight/Attack1/HeroKnight_Attack1_{i}.png")
    .ToArray();

_heroRunFrames = Enumerable.Range(0, 6)
    .Select(i => $"pack://application:,,,/Assets/Images/Hero/Hero Knight/Sprites/HeroKnight/Attack2/HeroKnight_Attack2_{i}.png")
    .ToArray();

// 添加第三个动画
private readonly IEnumerable<string> _heroRollFrames;

_heroRollFrames = Enumerable.Range(0, 9)
    .Select(i => $"pack://application:,,,/Assets/Images/Hero/Hero Knight/Sprites/HeroKnight/Roll/HeroKnight_Roll_{i}.png")
    .ToArray();
```

### 2. 添加资源到项目
编辑 `wpf_app/WorkHoursTimer/WorkHoursTimer.csproj`

添加新动画的资源引用：
```xml
<!-- Hero Attack2 Frames -->
<Resource Include="Assets\Images\Hero\Hero Knight\Sprites\HeroKnight\Attack2\HeroKnight_Attack2_0.png" />
<!-- ... 其他帧 ... -->

<!-- Hero Roll Frames -->
<Resource Include="Assets\Images\Hero\Hero Knight\Sprites\HeroKnight\Roll\HeroKnight_Roll_0.png" />
<!-- ... 其他帧 ... -->
```

### 3. 修改切换逻辑
在 `OnAnimationTimerElapsed` 方法中添加更多选择：

```csharp
// 随机切换勇者动画（3-4种动画）
var random = new Random();
var choice = random.Next(3); // 0, 1, 2
switch (choice)
{
    case 0:
        HeroFrames = _heroAttackFrames;  // Attack1
        break;
    case 1:
        HeroFrames = _heroRunFrames;     // Attack2
        break;
    case 2:
        HeroFrames = _heroRollFrames;    // Roll
        break;
}
```

---

## 🎯 快速修复建议

如果你觉得当前动画"奇怪"，最简单的修复：

### 立即改进：移除 Death 动画
将 Death 动画替换为 Roll（翻滚）或 Attack2（连击）

**原因**：
- Death 是死亡动画，不适合工作场景
- Roll 和 Attack2 更符合战斗主题

### 调整切换频率
将 5 秒改为 3 秒或 8 秒，找到最舒适的节奏

---

## 📝 需要我帮你实现哪个方案？

请告诉我：
1. 你觉得哪里"奇怪"？（动画本身？切换频率？组合？）
2. 你想要什么风格？（激烈战斗？轻松移动？多样化？）
3. 我可以立即帮你更换动画组合！
