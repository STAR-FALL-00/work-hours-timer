# Work Hours Timer v3.0 - Star Rail Refactor 开发计划

> **版本**: v3.0 (Star Rail Refactor)  
> **目标平台**: Windows 10 / 11  
> **核心架构**: 双窗口协同（侧边停靠栏 + 桌面像素挂件）  
> **预估工期**: 3-4 周  
> **创建日期**: 2026-02-26

---

## 📋 项目概述

### 愿景

将工时计时器从单窗口应用升级为双窗口协同系统，提供类似《崩坏：星穹铁道》的沉浸式 UI 体验，同时保留桌面像素挂件作为陪伴元素。

### 核心特性

1. **双窗口架构**: 主控制台（侧边停靠）+ 像素挂件（桌面悬浮）
2. **窗口特效**: 亚克力/云母毛玻璃背景
3. **毫秒级同步**: 跨窗口状态实时同步（<100ms）
4. **像素动画**: 勇者伐魔 / 跑酷猫咪两种皮肤
5. **数据兼容**: 完全兼容 v1.0-v1.4.0 的 Hive 数据

### 技术栈

```yaml
新增依赖:
  desktop_multi_window: ^0.2.0  # 多窗口管理
  flutter_acrylic: ^1.1.4       # 窗口特效
  win32: ^5.0.0                 # Windows API
  synchronized: ^3.1.0          # 并发控制
  rive: ^0.13.0                 # 动画引擎（可选）
  
保留依赖:
  flutter_riverpod: ^2.4.9      # 状态管理
  hive: ^2.2.3                  # 本地存储
  google_fonts: ^6.1.0          # 字体
  fl_chart: ^0.66.0             # 图表
```

---

## 🏗️ 系统架构设计

### 1. 双窗口模型


#### 窗口 A: 主控制台 (Main Dock)

**角色**: 控制器、数据中心、交互中心

**窗口属性**:
- 类型: 无边框窗口 (Frameless)
- 尺寸: 340px × 90% 屏幕高度
- 位置: 屏幕右侧边缘居中
- 背景: Acrylic/Mica 毛玻璃效果
- 特性: 支持自动隐藏/鼠标边缘唤出

**UI 布局**:
```
┌─────────────────────────┐
│  [Tab: Timer | Stats |  │  ← 顶部 Tab 导航
│         Settings]       │
├─────────────────────────┤
│                         │
│   [动态内容区域]         │  ← 根据 Tab 切换
│                         │
│                         │
├─────────────────────────┤
│  [🚀 START WORK]        │  ← 固定底部按钮
└─────────────────────────┘
```

**Tab 1: Timer (计时与任务)**
- 巨大的数字计时器（00:00:00）
- 滚动任务列表（项目选择）
- 每个项目显示：图标、名称、HP 进度条
- 选中项目外发光高亮

**Tab 2: Stats (数据面板)**
- 卡片 1: 本月薪资 & 时薪
- 卡片 2: 今日预计收益（滚动数字动画）
- 卡片 3: 本周工时趋势图

**Tab 3: Settings (设置)**
- 主题切换
- 挂件皮肤选择
- 开机自启
- 数据导出

#### 窗口 B: 像素挂件 (Pixel Widget)

**角色**: 纯展示、状态反馈、陪伴

**窗口属性**:
- 类型: 无边框、完全透明背景
- 尺寸: 240px × 120px（自适应）
- 位置: 屏幕右下角（可拖拽）
- 层级: Always On Top（始终置顶）
- 特性: 鼠标穿透（Click-through）

**皮肤模式**:

**皮肤 A: 勇者伐魔 (Boss Battle)**
```
┌──────────────────────────┐
│  🗡️ ⚔️  [HP ████░░] 👹  │
│  勇者    血条      Boss   │
└──────────────────────────┘
```
- 左侧: 勇者（Idle/Attack 动画）
- 右侧: Boss（Idle/Hit 动画）
- 中间: HP 血条（对应当日剩余工时）
- 逻辑: 工时增加 → 勇者攻击 → Boss 掉血

**皮肤 B: 跑酷猫咪 (Runner Cat)**
```
┌──────────────────────────┐
│      🐱💨               🏠│
│  [████████░░░░░░░░░░]   │
└──────────────────────────┘
```
- 底部: 蓝色进度条
- 上方: 猫咪（Run/Sleep 动画）
- 右侧: 城堡/家
- 逻辑: 工时增加 → 猫咪向右移动

---

## 📡 核心技术实现

### 2. 跨窗口通信协议

#### 2.1 通信架构

```dart
// 主窗口 (Main) ←→ 挂件窗口 (Widget)
// 使用 desktop_multi_window 的 MethodChannel

class WindowMessage {
  final String type;
  final Map<String, dynamic> data;
  final int timestamp;
}
```

#### 2.2 消息类型定义

**主窗口 → 挂件窗口**:

```dart
// 1. 更新计时器状态
{
  "type": "UPDATE_TIMER",
  "data": {
    "status": "running",      // idle | running | paused
    "duration": 3600,         // 已工作秒数
    "total": 28800           // 今日目标秒数
  }
}

// 2. 更新资源数据
{
  "type": "UPDATE_RESOURCES",
  "data": {
    "gold": 1500,
    "exp": 450,
    "level": 15
  }
}

// 3. 切换挂件皮肤
{
  "type": "SWITCH_SKIN",
  "data": {
    "skin_id": "boss_battle"  // boss_battle | runner_cat
  }
}

// 4. 控制挂件显隐
{
  "type": "WIDGET_VISIBILITY",
  "data": {
    "visible": true
  }
}
```

**挂件窗口 → 主窗口**:

```dart
// 1. 挂件位置变化（保存配置）
{
  "type": "WIDGET_MOVED",
  "data": {
    "x": 1000,
    "y": 800
  }
}

// 2. 请求显示主窗口
{
  "type": "REQUEST_SHOW_MAIN",
  "data": {}
}
```

#### 2.3 实现代码框架



```dart
// lib/core/services/window_communication_service.dart

class WindowCommunicationService {
  static final WindowCommunicationService _instance = 
      WindowCommunicationService._internal();
  factory WindowCommunicationService() => _instance;
  WindowCommunicationService._internal();

  int? _targetWindowId;

  /// 发送消息到目标窗口
  Future<void> sendMessage(String type, Map<String, dynamic> data) async {
    if (_targetWindowId == null) return;
    
    final message = {
      'type': type,
      'data': data,
      'timestamp': DateTime.now().millisecondsSinceEpoch,
    };
    
    await DesktopMultiWindow.invokeMethod(
      _targetWindowId!,
      'onMessage',
      message,
    );
  }

  /// 设置消息处理器
  void setMessageHandler(Function(String type, Map<String, dynamic> data) handler) {
    DesktopMultiWindow.setMethodHandler((call, fromWindowId) async {
      if (call.method == 'onMessage') {
        final message = call.arguments as Map<String, dynamic>;
        handler(message['type'], message['data']);
      }
    });
  }

  /// 设置目标窗口 ID
  void setTargetWindow(int windowId) {
    _targetWindowId = windowId;
  }
}
```

### 3. 数据持久化策略

#### 3.1 并发控制

**原则**: 主窗口写入，挂件窗口只读

```dart
// lib/core/services/data_sync_service.dart

class DataSyncService {
  final bool isMainWindow;
  
  DataSyncService({required this.isMainWindow});

  /// 写入数据（仅主窗口）
  Future<void> writeData(String key, dynamic value) async {
    if (!isMainWindow) {
      throw Exception('Widget window cannot write data');
    }
    
    await synchronized(() async {
      final box = Hive.box('app_data');
      await box.put(key, value);
    });
    
    // 通知挂件窗口数据已更新
    WindowCommunicationService().sendMessage('DATA_UPDATED', {
      'key': key,
      'value': value,
    });
  }

  /// 读取数据（所有窗口）
  T? readData<T>(String key) {
    final box = Hive.box('app_data');
    return box.get(key) as T?;
  }
}
```

#### 3.2 数据迁移

**兼容性**: 完全兼容 v1.0-v1.4.0

```dart
// 无需迁移，直接使用现有 Hive Box
await Hive.initFlutter('work_hours_timer');
final profileBox = await Hive.openBox<AdventurerProfile>('profile');
final recordsBox = await Hive.openBox<WorkRecord>('records');
```

### 4. 窗口特效实现

#### 4.1 主窗口毛玻璃效果

```dart
// lib/core/services/window_effect_service.dart

class WindowEffectService {
  Future<void> applyStarRailStyle() async {
    if (!Platform.isWindows) return;
    
    final version = await _getWindowsVersion();
    
    if (version >= 11) {
      // Windows 11: 云母效果
      await Window.setEffect(
        effect: WindowEffect.mica,
        dark: true,
      );
    } else {
      // Windows 10: 亚克力效果
      await Window.setEffect(
        effect: WindowEffect.acrylic,
        color: Color(0xCC1A1A2E),  // 星穹铁道深蓝色
      );
    }
  }

  Future<int> _getWindowsVersion() async {
    // 使用 win32 包获取 Windows 版本
    return 11; // 简化示例
  }
}
```

#### 4.2 挂件窗口透明 + 鼠标穿透

```dart
// lib/core/services/widget_window_service.dart

import 'package:win32/win32.dart';
import 'package:ffi/ffi.dart';

class WidgetWindowService {
  Future<void> setupTransparentWindow() async {
    // 1. 设置窗口透明
    await Window.setEffect(
      effect: WindowEffect.transparent,
    );
    
    // 2. 获取窗口句柄
    final hwnd = await _getWindowHandle();
    
    // 3. 设置鼠标穿透
    _setClickThrough(hwnd);
  }

  void _setClickThrough(int hwnd) {
    final exStyle = GetWindowLongPtr(hwnd, GWL_EXSTYLE);
    SetWindowLongPtr(
      hwnd,
      GWL_EXSTYLE,
      exStyle | WS_EX_TRANSPARENT | WS_EX_LAYERED,
    );
  }

  Future<int> _getWindowHandle() async {
    // 使用 window_manager 获取句柄
    return 0; // 简化示例
  }
}
```

---

## 🎨 UI 设计规范

### 5. 主窗口 UI 组件

#### 5.1 颜色系统（星穹铁道风格）

```dart
// lib/ui/theme/star_rail_colors.dart

class StarRailColors {
  // 主色调
  static const primary = Color(0xFF4A90E2);      // 星蓝
  static const secondary = Color(0xFFFFD700);    // 金色
  static const accent = Color(0xFFFF6B9D);       // 粉色
  
  // 背景色
  static const bgDark = Color(0xFF1A1A2E);       // 深蓝黑
  static const bgCard = Color(0xFF16213E);       // 卡片背景
  static const bgGlass = Color(0xCC1A1A2E);      // 毛玻璃
  
  // 状态色
  static const success = Color(0xFF4CAF50);
  static const warning = Color(0xFFFFA726);
  static const error = Color(0xFFEF5350);
  
  // 文字色
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFFB0B0B0);
  static const textHint = Color(0xFF707070);
}
```

#### 5.2 字体系统

```dart
// lib/ui/theme/star_rail_text_styles.dart

class StarRailTextStyles {
  // 数字字体（计时器）
  static TextStyle timerDisplay(Brightness brightness) {
    return GoogleFonts.orbitron(
      fontSize: 48,
      fontWeight: FontWeight.bold,
      color: StarRailColors.textPrimary,
      letterSpacing: 2,
    );
  }
  
  // 标题字体
  static TextStyle heading1(Brightness brightness) {
    return GoogleFonts.poppins(
      fontSize: 24,
      fontWeight: FontWeight.w600,
      color: StarRailColors.textPrimary,
    );
  }
  
  // 中文字体
  static TextStyle bodyChinese(Brightness brightness) {
    return TextStyle(
      fontFamily: 'MiSans',  // 需要添加到 pubspec.yaml
      fontSize: 14,
      color: StarRailColors.textSecondary,
    );
  }
}
```

#### 5.3 核心组件

**组件 1: 星轨 Tab 导航**

```dart
// lib/ui/widgets/star_rail_tab_bar.dart

class StarRailTabBar extends StatelessWidget {
  final List<TabItem> tabs;
  final int currentIndex;
  final Function(int) onTap;

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 60,
      decoration: BoxDecoration(
        gradient: LinearGradient(
          colors: [
            StarRailColors.bgCard.withOpacity(0.8),
            StarRailColors.bgCard.withOpacity(0.6),
          ],
        ),
        borderRadius: BorderRadius.circular(30),
      ),
      child: Row(
        children: tabs.asMap().entries.map((entry) {
          final isActive = entry.key == currentIndex;
          return _buildTabItem(entry.value, isActive);
        }).toList(),
      ),
    );
  }
}
```

**组件 2: 项目卡片（带 HP 条）**

```dart
// lib/ui/widgets/project_card_v3.dart

class ProjectCardV3 extends StatelessWidget {
  final Project project;
  final bool isSelected;
  final VoidCallback onTap;

  @override
  Widget build(BuildContext context) {
    return GestureDetector(
      onTap: onTap,
      child: AnimatedContainer(
        duration: Duration(milliseconds: 300),
        padding: EdgeInsets.all(16),
        decoration: BoxDecoration(
          color: StarRailColors.bgCard,
          borderRadius: BorderRadius.circular(12),
          border: Border.all(
            color: isSelected 
                ? StarRailColors.primary 
                : Colors.transparent,
            width: 2,
          ),
          boxShadow: isSelected ? [
            BoxShadow(
              color: StarRailColors.primary.withOpacity(0.5),
              blurRadius: 20,
              spreadRadius: 2,
            ),
          ] : [],
        ),
        child: Column(
          children: [
            // 项目名称
            Text(project.name, style: StarRailTextStyles.heading1(context.brightness)),
            SizedBox(height: 8),
            // HP 血条
            BossHealthBarV3(
              current: project.actualHours,
              max: project.estimatedHours,
            ),
          ],
        ),
      ),
    );
  }
}
```

### 6. 挂件窗口 UI 组件

#### 6.1 皮肤 A: 勇者伐魔

```dart
// lib/ui/widgets/skins/boss_battle_skin.dart

class BossBattleSkin extends StatefulWidget {
  final WorkStatus status;
  final double progress;  // 0.0 - 1.0

  @override
  _BossBattleSkinState createState() => _BossBattleSkinState();
}

class _BossBattleSkinState extends State<BossBattleSkin> 
    with TickerProviderStateMixin {
  late AnimationController _heroController;
  late AnimationController _bossController;

  @override
  void initState() {
    super.initState();
    _heroController = AnimationController(
      duration: Duration(milliseconds: 500),
      vsync: this,
    )..repeat();
    
    _bossController = AnimationController(
      duration: Duration(milliseconds: 800),
      vsync: this,
    );
  }

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 240,
      height: 120,
      child: Stack(
        children: [
          // 勇者（左侧）
          Positioned(
            left: 20,
            bottom: 20,
            child: _buildHeroSprite(),
          ),
          
          // Boss（右侧）
          Positioned(
            right: 20,
            bottom: 20,
            child: _buildBossSprite(),
          ),
          
          // HP 血条（中间）
          Positioned(
            left: 60,
            right: 60,
            top: 20,
            child: _buildHealthBar(),
          ),
        ],
      ),
    );
  }

  Widget _buildHeroSprite() {
    // 使用 Sprite Sheet 或 Rive 动画
    return AnimatedBuilder(
      animation: _heroController,
      builder: (context, child) {
        return Image.asset(
          'assets/sprites/hero_${widget.status == WorkStatus.working ? "attack" : "idle"}.png',
          width: 40,
          height: 40,
        );
      },
    );
  }

  Widget _buildBossSprite() {
    // Boss 动画
    return Image.asset(
      'assets/sprites/boss_idle.png',
      width: 40,
      height: 40,
    );
  }

  Widget _buildHealthBar() {
    return Container(
      height: 12,
      decoration: BoxDecoration(
        color: Colors.black.withOpacity(0.5),
        borderRadius: BorderRadius.circular(6),
        border: Border.all(color: Colors.white, width: 2),
      ),
      child: ClipRRect(
        borderRadius: BorderRadius.circular(4),
        child: LinearProgressIndicator(
          value: 1 - widget.progress,  // Boss 血量递减
          backgroundColor: Colors.transparent,
          valueColor: AlwaysStoppedAnimation(Colors.red),
        ),
      ),
    );
  }
}
```

#### 6.2 皮肤 B: 跑酷猫咪

```dart
// lib/ui/widgets/skins/runner_cat_skin.dart

class RunnerCatSkin extends StatefulWidget {
  final WorkStatus status;
  final double progress;  // 0.0 - 1.0

  @override
  _RunnerCatSkinState createState() => _RunnerCatSkinState();
}

class _RunnerCatSkinState extends State<RunnerCatSkin> {
  @override
  Widget build(BuildContext context) {
    return Container(
      width: 240,
      height: 120,
      child: Stack(
        children: [
          // 进度条（底部）
          Positioned(
            left: 10,
            right: 10,
            bottom: 10,
            child: _buildProgressTrack(),
          ),
          
          // 猫咪（根据进度移动）
          Positioned(
            left: 10 + (200 * widget.progress),
            bottom: 30,
            child: _buildCatSprite(),
          ),
          
          // 城堡（右侧）
          Positioned(
            right: 10,
            bottom: 30,
            child: _buildCastle(),
          ),
        ],
      ),
    );
  }

  Widget _buildProgressTrack() {
    return Container(
      height: 16,
      decoration: BoxDecoration(
        color: Color(0xFF2196F3).withOpacity(0.3),
        borderRadius: BorderRadius.circular(8),
      ),
      child: ClipRRect(
        borderRadius: BorderRadius.circular(8),
        child: LinearProgressIndicator(
          value: widget.progress,
          backgroundColor: Colors.transparent,
          valueColor: AlwaysStoppedAnimation(Color(0xFF2196F3)),
        ),
      ),
    );
  }

  Widget _buildCatSprite() {
    return Image.asset(
      'assets/sprites/cat_${widget.status == WorkStatus.working ? "run" : "sleep"}.png',
      width: 32,
      height: 32,
    );
  }

  Widget _buildCastle() {
    return Image.asset(
      'assets/sprites/castle.png',
      width: 40,
      height: 40,
    );
  }
}
```

---

## 📅 开发路线图（3 个 Sprint）



### Sprint 1: 框架与通信（Week 1）

**目标**: 跑通双窗口 "Hello World"

#### Day 1-2: 环境搭建

**任务**:
- [ ] 添加新依赖到 `pubspec.yaml`
- [ ] 初始化 `desktop_multi_window`
- [ ] 创建窗口启动逻辑

**代码结构**:
```
lib/
├── main.dart                          # 主入口
├── main_window.dart                   # 主窗口入口
├── widget_window.dart                 # 挂件窗口入口
└── core/
    └── services/
        ├── window_manager_service.dart
        └── window_communication_service.dart
```

**验收标准**:
- ✅ 运行应用后，同时出现两个窗口
- ✅ 主窗口可以通过按钮控制挂件窗口显隐

#### Day 3-4: 窗口特效

**任务**:
- [ ] 集成 `flutter_acrylic`
- [ ] 实现主窗口亚克力背景
- [ ] 实现挂件窗口透明背景
- [ ] 实现鼠标穿透（Win32 API）

**验收标准**:
- ✅ 主窗口显示毛玻璃效果
- ✅ 挂件窗口完全透明
- ✅ 点击挂件空白处可以穿透到桌面

#### Day 5-7: 跨窗口通信

**任务**:
- [ ] 实现 `WindowCommunicationService`
- [ ] 测试消息发送和接收
- [ ] 测试延迟（目标 <100ms）

**验收标准**:
- ✅ 主窗口点击按钮 → 挂件窗口显示文字变化
- ✅ 延迟测试通过（<100ms）

**交付物**:
- 双窗口 Demo 应用
- 通信延迟测试报告

---

### Sprint 2: 业务迁移与绑定（Week 2）

**目标**: 复活 v1.0 的功能

#### Day 8-10: 数据层集成

**任务**:
- [ ] 实现 `DataSyncService`（并发控制）
- [ ] 接入 Hive 数据库
- [ ] 读取 v1.4.0 的数据
- [ ] 测试数据兼容性

**验收标准**:
- ✅ 可以读取旧版的工作记录
- ✅ 可以读取旧版的冒险者档案
- ✅ 主窗口写入数据后，挂件窗口可以读取

#### Day 11-12: 计时器逻辑

**任务**:
- [ ] 在主窗口实现计时器逻辑
- [ ] 通过 MethodChannel 推送计时数据到挂件
- [ ] 挂件窗口接收数据并更新 UI

**验收标准**:
- ✅ 主窗口点击"开始工作" → 挂件显示计时
- ✅ 计时数据实时同步（每秒更新）

#### Day 13-14: 基础 UI 实现

**任务**:
- [ ] 主窗口：实现 Tab 切换框架
- [ ] 主窗口：实现 Timer Tab 的基础 UI
- [ ] 挂件窗口：实现简单的进度条

**验收标准**:
- ✅ 主窗口可以切换 3 个 Tab
- ✅ Timer Tab 显示计时器和项目列表
- ✅ 挂件窗口显示进度条（根据工时变化）

**交付物**:
- 功能完整的双窗口应用（基础版）
- 数据兼容性测试报告

---

### Sprint 3: UI 抛光与动画（Week 3）

**目标**: 还原设计稿，注入灵魂

#### Day 15-17: 主窗口 UI 完善

**任务**:
- [ ] 实现星穹铁道风格的 Tab 导航
- [ ] 实现项目卡片（带外发光效果）
- [ ] 实现 Stats Tab（图表 + 卡片）
- [ ] 实现 Settings Tab

**验收标准**:
- ✅ UI 还原度 ≥ 90%
- ✅ 所有交互动画流畅（60 FPS）

#### Day 18-19: 挂件动画实现

**任务**:
- [ ] 准备像素素材（Sprite Sheets）
- [ ] 实现勇者伐魔皮肤
- [ ] 实现跑酷猫咪皮肤
- [ ] 实现皮肤切换逻辑

**验收标准**:
- ✅ 勇者会根据工作状态切换动画（Idle/Attack）
- ✅ Boss 血条会随工时减少
- ✅ 猫咪会根据进度移动

#### Day 20-21: 细节打磨

**任务**:
- [ ] 添加音效（按钮点击、金币获得）
- [ ] 添加数字滚动动画
- [ ] 添加窗口滑入滑出动画
- [ ] 性能优化

**验收标准**:
- ✅ 所有交互都有音效反馈
- ✅ 金币/经验值变化有滚动动画
- ✅ 主窗口隐藏/显示有平滑动画
- ✅ 内存占用 < 200MB（两个窗口总和）

**交付物**:
- v3.0 完整版应用
- 用户使用指南
- 性能测试报告

---

## 📂 资源清单

### 字体资源

```yaml
# pubspec.yaml
fonts:
  - family: MiSans
    fonts:
      - asset: assets/fonts/MiSans-Regular.ttf
      - asset: assets/fonts/MiSans-Bold.ttf
        weight: 700
```

**下载地址**:
- Poppins: Google Fonts（自动下载）
- Orbitron: Google Fonts（自动下载）
- MiSans: https://hyperos.mi.com/font

### 图标资源

```yaml
# pubspec.yaml
dependencies:
  remixicon: ^1.0.0
```

### 动画素材

**需要准备的 Sprite Sheets**:

1. **勇者（Hero）**:
   - `hero_idle.png` - 站立动画（4 帧）
   - `hero_attack.png` - 攻击动画（6 帧）
   - 尺寸: 每帧 32×32px

2. **Boss**:
   - `boss_idle.png` - 站立动画（4 帧）
   - `boss_hit.png` - 受击动画（3 帧）
   - 尺寸: 每帧 40×40px

3. **猫咪（Cat）**:
   - `cat_run.png` - 奔跑动画（6 帧）
   - `cat_sleep.png` - 睡觉动画（4 帧）
   - 尺寸: 每帧 32×32px

4. **场景**:
   - `castle.png` - 城堡（静态）
   - `home.png` - 家（静态）

**推荐素材来源**:
- itch.io（免费像素素材）
- OpenGameArt.org
- Kenney.nl

**AI 生成提示词**:
```
"pixel art, 8-bit style, knight character sprite sheet, 
idle and attack animation, transparent background, 32x32 pixels"
```

---

## 🎯 验收标准

### 功能验收

- [ ] **双窗口启动**: 应用启动后，主窗口和挂件窗口同时显示
- [ ] **窗口特效**: 主窗口显示毛玻璃效果，挂件窗口完全透明
- [ ] **鼠标穿透**: 点击挂件空白处可以穿透到桌面
- [ ] **状态同步**: 主窗口修改数据后，挂件窗口 <100ms 内更新
- [ ] **计时功能**: 点击"开始工作"后，挂件显示计时和动画
- [ ] **数据兼容**: 可以读取 v1.4.0 的所有数据
- [ ] **皮肤切换**: 可以在设置中切换挂件皮肤
- [ ] **窗口拖拽**: 挂件窗口可以拖拽，位置会被保存

### 性能验收

- [ ] **FPS**: 主窗口 ≥ 60 FPS，挂件窗口 ≥ 30 FPS
- [ ] **内存**: 两个窗口总内存 < 200MB
- [ ] **启动时间**: 应用启动到显示 < 3 秒
- [ ] **通信延迟**: 跨窗口消息延迟 < 100ms

### UI 验收

- [ ] **还原度**: UI 还原度 ≥ 90%（对比设计稿）
- [ ] **动画流畅**: 所有动画 60 FPS 无卡顿
- [ ] **响应式**: 主窗口在不同屏幕高度下正常显示

---

## 🚨 风险与缓解

### 风险 1: 状态同步延迟超过预期

**概率**: 中  
**影响**: 高

**缓解措施**:
1. 提前做性能测试（Sprint 1）
2. 如果延迟 >100ms，考虑降低同步频率（每 2 秒同步一次）
3. 备选方案：使用 Hive 监听（延迟 200ms 可接受）

### 风险 2: 多窗口内存占用过高

**概率**: 中  
**影响**: 中

**缓解措施**:
1. 挂件窗口使用低刷新率（30 FPS）
2. 只加载必要的资源
3. 提供"单窗口模式"作为备选

### 风险 3: 像素素材准备不足

**概率**: 低  
**影响**: 中

**缓解措施**:
1. 提前准备素材清单
2. 使用 AI 生成工具（Midjourney/DALL-E）
3. 备选方案：使用简单的几何图形代替

### 风险 4: Windows 版本兼容性问题

**概率**: 低  
**影响**: 高

**缓解措施**:
1. 在 Windows 10 和 Windows 11 上分别测试
2. 提供降级方案（Windows 10 使用 Acrylic，Windows 11 使用 Mica）
3. 如果特效不支持，回退到纯色背景

---

## 📝 开发规范

### 代码规范

1. **命名规范**:
   - 文件名: `snake_case`
   - 类名: `PascalCase`
   - 变量名: `camelCase`
   - 常量名: `UPPER_SNAKE_CASE`

2. **注释规范**:
   - 所有 public 方法必须有文档注释
   - 复杂逻辑必须有行内注释

3. **提交规范**:
   - `feat: 添加新功能`
   - `fix: 修复 bug`
   - `refactor: 重构代码`
   - `docs: 更新文档`

### 测试规范

1. **单元测试**: 核心服务必须有单元测试
2. **集成测试**: 跨窗口通信必须有集成测试
3. **性能测试**: 每个 Sprint 结束后进行性能测试

---

## 📞 技术支持

### 参考文档

- [desktop_multi_window 官方文档](https://pub.dev/packages/desktop_multi_window)
- [flutter_acrylic 官方文档](https://pub.dev/packages/flutter_acrylic)
- [Win32 API 文档](https://learn.microsoft.com/en-us/windows/win32/)

### 示例项目

- [Biyi (比译)](https://github.com/biyidev/biyi) - 多窗口翻译软件
- [Fluent UI](https://github.com/bdlukaa/fluent_ui) - Windows 11 风格 UI

---

## 🎉 总结

v3.0 是一次重大的架构升级，从单窗口应用升级为双窗口协同系统。虽然工作量较大（3-4 周），但技术可行性高，所有核心技术都有成熟的解决方案。

**关键成功因素**:
1. ✅ 提前做好技术验证（Sprint 1）
2. ✅ 严格控制状态同步延迟
3. ✅ 注重 UI 还原度和动画流畅度
4. ✅ 做好性能优化和内存控制

**预期成果**:
- 一个具有星穹铁道风格的沉浸式工时计时器
- 双窗口协同，提供更好的用户体验
- 完全兼容 v1.0-v1.4.0 的数据

---

**创建日期**: 2026-02-26  
**状态**: 📋 计划中  
**下一步**: 开始 Sprint 1 - 框架与通信

祝开发顺利！🚀
